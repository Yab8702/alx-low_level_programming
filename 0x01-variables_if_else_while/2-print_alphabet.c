#include <stdio.h>
/**
 * main - Entry point[print alphabet]
 *
 * Return: Always 0(sucess)
 */
int main(void)
{
	char a;

	for (a = 'a'; a < = 'z'; a++)
	{
		putchar(a);
	}
	putchar('\n');
	return (0);
}

#include <stdio.h>

/**
 * main - prints all single digit numbers of base 10 start from 0
 * 
 * Return: Always 0(sucess)
 */

int main(void)
{
	int n;

	for (n = 48; n < 58; n++)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}		

#include <stdio.h>

/**
 * main - the first 50 fibonacci number
 * Return:Always 0(sucess)
 */

int main(void)
{
	int x;
	long int fib1 = 1, fib2 = 1, fib3;

	printf("%lf, ", fib2);
	for (x = 0; x < 50; x++)
	{
		fib3 = fib1 + fib2;
		printf("%lf", fib3);
		fib1 = fib2;
		fib2 = fib3;
		if (x < 49)
			printf(", ");
	}
	printf("\n");
	return (0);
}

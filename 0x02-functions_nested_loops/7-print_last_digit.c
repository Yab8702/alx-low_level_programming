#include "main.h"


/**
 * print_last_digit - print the last digit of a number
 * @n:the number that check
 * Return: always last digit (sucess)
 */


int print_last_digit(int n)
{
	int last_digit = n % 10;

	return (last_digit);
}

#include "main.h"

/**
 * add - add two numbers
 * @a: the first number to add
 * @b: the second number to add
 * Return: result(sucess)
 */

int add(int a, int b)
{
	int result;

	result = a + b;

	return (result);
}
